--- dde-daemon-3.0.11/systeminfo/distro.go.orig	2016-03-08 12:46:18.704427055 +0800
+++ dde-daemon-3.0.11/systeminfo/distro.go	2016-03-08 14:14:27.544950224 +0800
@@ -14,16 +14,16 @@
 )
 
 const (
-	distroFileLSB    = "/etc/lsb-release"
+	OSReleaseFile    = "/etc/os-release"
 
-	distroIdKeyLSB   = "DISTRIB_ID"
-	distroDescKeyLSB = "DISTRIB_DESCRIPTION"
-	distroVerKeyLSB  = "DISTRIB_RELEASE"
+	distroName   = "NAME"
+	distroPrettyName = "PRETTY_NAME"
+	distroVerID  = "VERSION_ID"
 	distroKeyDelim   = "="
 )
 
 func getDistro() (string, string, string, error) {
-	distroId, distroDesc, distroVer, err := getDistroFromLSB(distroFileLSB)
+	distroId, distroDesc, distroVer, err := getDistroFromOSRelease(OSReleaseFile)
 	if err == nil {
 		return distroId, distroDesc, distroVer, nil
 	}
@@ -31,29 +31,29 @@
 	return "", "", "", err
 }
 
-func getDistroFromLSB(file string) (string, string, string, error) {
+func getDistroFromOSRelease(file string) (string, string, string, error) {
 	ret, err := parseInfoFile(file, distroKeyDelim)
 	if err != nil {
 		return "", "", "", err
 	}
 
-	distroId, ok := ret[distroIdKeyLSB]
+	distroId, ok := ret[distroName]
 	if !ok {
-		return "", "", "", fmt.Errorf("Cannot find the key '%s'", distroIdKeyLSB)
+		return "", "", "", fmt.Errorf("Cannot find the key '%s'", distroName)
 	}
 
-	distroDesc, ok := ret[distroDescKeyLSB]
+	distroDesc, ok := ret[distroPrettyName]
 	if !ok {
-		return "", "", "", fmt.Errorf("Cannot find the key '%s'", distroDescKeyLSB)
+		return "", "", "", fmt.Errorf("Cannot find the key '%s'", distroPrettyName)
 	}
 
 	if distroDesc[0] == '"' && distroDesc[len(distroDesc) - 1] == '"' {
 		distroDesc = distroDesc[1:len(distroDesc) - 1]
 	}
 
-	distroVer, ok := ret[distroVerKeyLSB]
+	distroVer, ok := ret[distroVerID]
 	if !ok {
-		return "", "", "", fmt.Errorf("Cannot find the key '%s'", distroVerKeyLSB)
+		return distroId, distroDesc, "", nil
 	}
 
 	return distroId, distroDesc, distroVer, nil
--- dde-daemon-3.0.11/systeminfo/version.go.orig	2016-03-16 23:21:55.666339375 +0800
+++ dde-daemon-3.0.11/systeminfo/version.go	2016-03-16 23:22:08.664316034 +0800
@@ -40,7 +40,7 @@
 		return version, nil
 	}
 
-	return "", err
+	return "2015", err
 }
 
 func getVersionFromDeepin(file string) (string, error) {
